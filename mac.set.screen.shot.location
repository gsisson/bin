#!/usr/bin/env bash

set -x
#defaults write com.apple.screencapture location $HOME/Desktop
 defaults write com.apple.screencapture location $HOME/Dropbox/work/Screenshots
 killall SystemUIServer
 set +x
 echo 'wait a bit, and then screen shots should go to that folder...'
