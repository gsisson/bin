#!/usr/bin/env bash

cat << EOF
# merge
  git checkout master
  git pull --rebase
  git merge feature_branch
# push
  git push
# delete branch
  git branch -d feature_branch
  git push origin --delete feature_branch
EOF
